name: Simple Push Workflow

on:
  push:
    branches:
      - main
    tags:
      - '*'           # triggers on any tag push
    paths:
      - 'src/**'
      - 'package.json'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
